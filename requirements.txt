# Company Voice Agent - Python Dependencies
# Install all with: pip install -r requirements.txt

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Azure OpenAI
openai==1.12.0

# Azure Speech Services
# Version 1.47.0+ required for OpenSSL 3.0 compatibility (fixes Error 2176)
azure-cognitiveservices-speech>=1.47.0

# Vector Store (ChromaDB)
chromadb==0.4.22
numpy<2.0  # Required for chromadb compatibility

# Database
sqlalchemy==2.0.25
pymysql==1.1.0
cryptography==42.0.2

# Environment Variables
python-dotenv==1.0.0

# PDF Processing (for knowledge base upload)
pymupdf==1.23.22  # Required for PDF processing (import fitz)
pdfplumber==0.10.4  # Fallback for PDF processing

# Utilities
pydantic==2.5.3
# pydantic-settings==2.1.0  # Unused

# CORS
starlette==0.35.1

# Async Support
httpx==0.26.0
anyio==4.2.0

# WebSocket
websockets==12.0

# Audio (for client scripts)
sounddevice==0.4.6
